# Day 5 - Interactive Water Heating Model

## Objective

Make the water-heating digital twin interactive by allowing the user to provide the heater power as an input.

## New Concept

Instead of using a fixed heater power, the model accepts the heater power from the user.

Example:

```python
heater_power = float(input("Enter heater power (W): "))